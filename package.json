{
  "name": "cyber-stream-sdk",
  "version": "1.0.21",
  "description": "An SDK for exploring IMDB movies and shows.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsup src/index.ts && tsc --emitDeclarationOnly --declaration --declarationMap"
  },
  "keywords": [
    "cyber",
    "stream",
    "movie",
    "show",
    "imdb"
  ],
  "author": "tomide adeoye",
  "license": "MIT",
  "devDependencies": {
    "tsup": "^8.0.2"
  },
  "dependencies": {
    "typescript": "^5.0.4"
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ]
}
